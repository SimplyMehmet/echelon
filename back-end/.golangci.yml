version: "2"
run:
  tests: true
  timeout: 5m
formatters:
  disable-all: true
  enable:
    - gofmt
    - goimports
linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - gocritic
    - gosec
    - errcheck
    - unused
    - unconvert
    - goconst
    - gocyclo
    - revive