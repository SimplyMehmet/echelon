To run this project make sure docker is installed and make command is available.

after to run the apps simply run the command:

`make`

api - port `8080`
ui - port `3000`
db - port `3306`

to generate graphql client run command `go run github.com/Khan/genqlient` in the client folder

Happy coding!!!
